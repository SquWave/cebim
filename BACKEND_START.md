# Backend Server Başlatma

Backend server'ı başlatmak için **yeni bir terminal** açın ve şu komutu çalıştırın:

```bash
node server.js
```

Şunu görmelisiniz:
```
🚀 Cebim Backend Proxy running on http://localhost:3001
📊 Stock data endpoint: http://localhost:3001/api/stocks
💱 FX data endpoint: http://localhost:3001/api/fx
```

Backend çalıştıktan sonra frontend otomatik olarak bağlanacak ve autocomplete çalışacak!
